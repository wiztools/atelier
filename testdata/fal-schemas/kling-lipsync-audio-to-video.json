{
  "components": {
    "schemas": {
      "KlingVideoLipsyncAudioToVideoInput": {
        "type": "object",
        "x-fal-order-properties": ["video_url", "audio_url"],
        "required": ["video_url", "audio_url"],
        "properties": {
          "video_url": { "type": "string", "description": "The URL of the source video whose lips to re-sync." },
          "audio_url": { "type": "string", "description": "The URL of the driving audio." }
        }
      }
    }
  }
}
